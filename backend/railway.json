{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 4 --worker-class gthread run:app",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 1000,
    "restartPolicyType": "ON_FAILURE"
  }
}